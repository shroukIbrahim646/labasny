import 'package:flutter/material.dart';
class DiscountPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Discount Page'),
      ),
      body: Center(
        child: Text('This is the Discount Page'),
      ),
    );
  }
}