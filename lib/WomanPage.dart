import 'package:flutter/material.dart';
class WomanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Woman Page'),
      ),
      body: Center(
        child: Text('This is the Woman Page'),
      ),
    );
  }
}

