import 'package:flutter/material.dart';
class ManPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Man Page'),
      ),
      body: Center(
        child: Text('This is the Man Page'),
      ),
    );
  }
}

